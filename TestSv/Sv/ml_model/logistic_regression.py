# import pandas as pd
# from sklearn.linear_model import LogisticRegression

# df = pd.read_excel("static/linear_data.xlsx", sheet_name=1)
# df = df.drop_duplicates(keep='first')

# x_train = df.drop("ShouldGo", axis="columns")

# y_train = df.ShouldGo

# lgr_model = LogisticRegression()

# lgr_model.fit(x_train.values,y_train.values)

